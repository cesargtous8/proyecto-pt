{
  "scripts": {
    "ng": "ng",
    "start": "ng serve --host 0.0.0.0 --port 4200",
    "build": "ng build",
    "build:prod": "ng build --configuration=production",
    "test": "ng test",
    "test:coverage": "ng test --code-coverage",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "docker:build": "docker build -t fullstack-frontend .",
    "docker:run": "docker run -p 80:80 fullstack-frontend"
  },
  "dependencies": {
    "@angular/flex-layout": "^14.0.0-beta.41"
  }
}
